{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-I",
                "${workspaceFolder}/include",
                "${workspaceFolder}/src/*",
                "-o",
                "${workspaceFolder}/bin/app",
                "-lpthread",
                "-lGL",
                "-ldl",
                "-lX11",
                "-lglfw3"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}